# UE 5.7 Gameplay Framework → Unity 复刻执行手册

> 本文件是交给项目 Agent 的执行契约。目标不是逐行翻译 Unreal Engine，而是在 Unity 中复刻 `Controller / PlayerState / Character` 的职责边界、生命周期、控制权与网络移动行为。

## 1. Agent 的任务

在目标 Unity 项目中实现一套可扩展的 Gameplay Framework，至少包含：

1. 可在人类玩家、AI 与不同身体之间切换的 Controller/Possession 系统。
2. 不随 Character 死亡而销毁、可供全场观察的 PlayerState。
3. 由胶囊碰撞、视觉模型和独立 CharacterMotor 组成的 Character。
4. 清晰区分服务器、拥有客户端和远端观察者的网络同步路径。
5. 分阶段实现服务器权威、远端插值，以及拥有客户端的预测、纠错和未确认输入回放。

目标 UE 源码根目录：

```text
C:\Program Files\Epic Games\UE_5.7\Engine\Source
```

目标 Unity 项目路径需要由用户提供，或由 Agent 在当前工作区中定位。不得在 UE 源码目录内创建 Unity 项目或修改 UE 文件。

## 2. 核心架构原则

必须保持下面三种生命周期分离：

| 对象 | 生命周期 | 保存什么 | 不保存什么 |
|---|---|---|---|
| Controller | 跟玩家连接或 AI 决策者走 | 当前 Pawn、控制方向、输入/AI 意图、镜头和控制状态 | 生命、碰撞、Animator、全场公开玩家资料 |
| PlayerState | 跟玩家身份走 | 玩家名、账号 ID、局内 ID、队伍、比分、Ping、观战状态 | Transform、速度、移动模式、Animator、临时 Buff |
| Character | 跟本次出生的身体走 | 胶囊、Mesh、生命、动作、速度、移动模式、根运动 | 跨重生的比分、账号状态、设备输入源 |

必须维持以下不变量：

- 一个 Controller 同时最多拥有一个 Pawn。
- 一个 Pawn 同时最多由一个 Controller 控制。
- Possess/UnPossess 必须更新双向引用、网络 ownership、输入与相机生命周期。
- 角色死亡通常只销毁 Character；Controller 与 PlayerState 保留。
- `ControlRotation` 与 Character 的 Transform rotation 是两个独立状态。
- 输入回调只提交意图，不能直接修改角色 Transform。
- Character 的权威位置来自胶囊碰撞根，不来自 Animator 或 Mesh。
- 客户端发送输入命令，服务器重演输入；服务器不直接相信客户端 Transform。
- owner client 使用预测/纠错，remote observer 使用快照/插值，两条路径不得混用。

## 3. 必须掌握的知识点与 UE 源码入口

以下路径均相对于 UE 源码根目录。

### 3.1 Controller、Pawn 与控制权

| 知识点 | 必须理解 | 源码入口 | Unity 产物 |
|---|---|---|---|
| Controller/Pawn 分离 | 决策者为什么不是身体 | `Runtime/Engine/Classes/GameFramework/Controller.h`，类说明和 Pawn/PlayerState/ControlRotation 字段 | `IController`、`IPossessable` |
| PlayerController 网络拓扑 | 它只存在于服务器和拥有客户端，不存在于远端观察客户端 | `Runtime/Engine/Classes/GameFramework/PlayerController.h:247` | owner-only `LocalPlayerController` |
| Possess 权限 | 默认只有 authority 可以改变占有 | `Runtime/Engine/Private/Controller.cpp:316`，`AController::Possess` | server-only `PossessionCoordinator` |
| 双向关系建立 | 先释放旧 Pawn/Controller，再建立新关系 | `Runtime/Engine/Private/Controller.cpp:352`，`AController::OnPossess` | 原子化 `Possess()` 流程 |
| Pawn 侧所有权 | Owner、Controller、PlayerState、owning connection、AutonomousProxy 如何更新 | `Runtime/Engine/Private/Pawn.cpp:650`，`APawn::PossessedBy` | `OnPossessed` 与网络 ownership 更新 |
| UnPossess 清理 | 清空 PlayerState、Owner、Controller、InputComponent 与输入缓存 | `Runtime/Engine/Private/Pawn.cpp:692`，`APawn::UnPossessed` | `OnUnpossessed` 和资源解绑 |
| Pawn 重启 | 为什么本地占有完成后才创建输入组件 | `Runtime/Engine/Private/Pawn.cpp:475`，`PawnClientRestart` | `BindLocalInput()` 生命周期 |
| 占有握手 | `ClientRestart → AcknowledgePossession → ServerAcknowledgePossession` | `Runtime/Engine/Private/PlayerController.cpp:820`、`:877`、`:933`、`:1327` | `PossessGranted/PossessReady` 协议 |
| 复制乱序 | Pawn.Controller 和 Controller.Pawn 可能先后到达 | `Runtime/Engine/Private/Pawn.cpp`，`APawn::OnRep_Controller`；`Runtime/Engine/Private/Controller.cpp:538` | 引用解析与幂等修复 |
| ControlRotation | 瞄准/观察方向与身体旋转分离 | `Runtime/Engine/Private/Controller.cpp:128`；`Runtime/Engine/Private/PlayerController.cpp:1037` | 独立 `Quaternion ControlRotation` |
| 输入栈 | 本地 PlayerController 如何构建与处理 InputComponent 优先级 | `Runtime/Engine/Private/PlayerController.cpp:2308`、`:2675`、`:2768` | Unity Input System action maps/输入上下文 |
| 玩家和 AI 共用协议 | PlayerController 与 AIController 都通过 Controller 驱动 Pawn | `Runtime/AIModule/Classes/AIController.h`；`Runtime/AIModule/Private/AIController.cpp` | `LocalPlayerController`、`AiController` |

### 3.2 PlayerState 与玩家生命周期

| 知识点 | 必须理解 | 源码入口 | Unity 产物 |
|---|---|---|---|
| PlayerState 职责 | 为什么比分和玩家身份不能放 Character | `Runtime/Engine/Classes/GameFramework/PlayerState.h` 类说明 | `PlayerRuntimeState` / `NetworkPlayerState` |
| 创建位置 | Controller 根据 GameMode 的 PlayerStateClass 在服务器创建 | `Runtime/Engine/Private/Controller.cpp:623`，`InitPlayerState` | server-side player session factory |
| 全局可见性 | `bReplicates`、`bAlwaysRelevant`、关闭移动复制、低频更新 | `Runtime/Engine/Private/PlayerState.cpp:16` | 全场可观察的低频网络对象 |
| 玩家索引 | PlayerState 如何加入/离开 GameState.PlayerArray | `Runtime/Engine/Private/PlayerState.cpp:135`；`Runtime/Engine/Private/GameStateBase.cpp:122` | `PlayerRegistry` |
| 核心字段 | Score、PlayerId、UniqueId、PlayerName、Ping、观战状态的差异 | `Runtime/Engine/Classes/GameFramework/PlayerState.h:47` | 分离 Match ID 与 Account ID |
| Push Model | Setter 为什么必须标记 property dirty | `Runtime/Engine/Private/PlayerState.cpp:422` | 统一 server mutation API |
| 复制条件 | `InitialOnly`、`SkipOwner`、持续复制字段 | `Runtime/Engine/Private/PlayerState.cpp:397` | 每字段的可见性与更新策略 |
| OnRep | 网络变化如何转换成 UI、玩家列表和会话副作用 | `Runtime/Engine/Private/PlayerState.cpp:180`、`:238`、`:328` | C# event / NetworkVariable callback |
| Ping 压缩 | 四个一秒桶、滑动平均、除 4 压入 byte | `Runtime/Engine/Private/PlayerState.cpp:39` | 精确本地 Ping + 压缩远端 Ping |
| Pawn 反向缓存 | PawnPrivate 不是持久化或复制字段 | `Runtime/Engine/Private/PlayerState.cpp:514`；`Runtime/Engine/Private/Pawn.cpp:626` | 非权威运行时反向引用 |
| 无缝旅行 | CopyProperties 白名单迁移 | `Runtime/Engine/Private/PlayerState.cpp:91`、`:112`、`:387` | `TransferPersistentFields` |
| 掉线重连 | Duplicate、InactivePlayerArray、OverrideWith、重新激活 | `Runtime/Engine/Private/GameMode.cpp:601` 至 `:750` | 有寿命和容量限制的 reconnect cache |

### 3.3 Character 与移动模拟

| 知识点 | 必须理解 | 源码入口 | Unity 产物 |
|---|---|---|---|
| Character 组合 | 胶囊是碰撞根，Mesh 是视觉子节点，Movement 移动胶囊 | `Runtime/Engine/Private/Character.cpp:54` | `CharacterAvatar` prefab 层级 |
| 输入缓冲 | AddMovementInput 累加，一次模拟消费并清零 | `Runtime/Engine/Private/Pawn.cpp:799` | `PendingInput` / `ConsumeInput` |
| 移动 Tick | authority、autonomous proxy、simulated proxy 分流 | `Runtime/Engine/Private/Components/CharacterMovementComponent.cpp:1622` | 固定模拟 Tick 与 role dispatch |
| 输入到加速度 | Constrain、Scale、AnalogInputModifier | `CharacterMovementComponent.cpp:6340`，`ControlledCharacterMove` | `MoveIntent → Acceleration` |
| PerformMovement | 力、跳跃、蹲伏、根运动和物理的统一入口 | `CharacterMovementComponent.cpp:2703` | `CharacterMotor.Simulate()` |
| MovementMode | Walking/Falling/Flying/Swimming/Custom 状态机 | `CharacterMovementComponent.cpp:3454`，`StartNewPhysics` | 可序列化 `MovementMode` |
| 行走物理 | 地板检测、摩擦、制动、斜坡、台阶、移动平台 | `CharacterMovementComponent.cpp:5554`，`PhysWalking` | kinematic capsule motor |
| 跳跃 | 请求标志、次数、持有时间、向上速度、切换 Falling | `Runtime/Engine/Private/Character.cpp:1157`；`CharacterMovementComponent.cpp:1120` | 可回放 Jump 状态 |
| 蹲伏 | 改权威胶囊高度，起立前做空间查询 | `CharacterMovementComponent.cpp:3185` | collider resize + obstruction check |
| 根运动 | 动画根位移转换成速度并继续走碰撞 | `CharacterMovementComponent.cpp:2842`、`:4432` | root motion extraction adapter |
| 移动平台 | BasedMovement 的相对坐标与参考系 | `Runtime/Engine/Private/Character.cpp:1252` | `MovementBaseId + relative pose` |
| 模拟代理 | 复制离散状态、本地外推与平滑 | `Runtime/Engine/Private/Character.cpp:1610`；`CharacterMovementComponent.cpp:2159` | observer snapshot interpolation |

### 3.4 网络预测与纠错

| 知识点 | 必须理解 | 源码入口 | Unity 产物 |
|---|---|---|---|
| SavedMove | 保存时间戳、量化输入、动作标志、模式、基座和起止状态 | `CharacterMovementComponent.cpp:12502` 附近 | `MoveCommand` 与预测环形缓冲 |
| 本地预测 | 创建 SavedMove 后立即 PerformMovement | `CharacterMovementComponent.cpp:8789`，`ReplicateMoveToServer` | owner-side immediate simulation |
| Move 合并 | 仅在关键状态兼容时合并 | `ReplicateMoveToServer` 和 `FSavedMove_Character::CanCombineWith` | 可选 command batching |
| 输入量化 | 客户端预测必须使用网络发送后的量化值 | `FSavedMove_Character::SetMoveFor` | shared quantizer |
| Packed RPC | 将 New/Pending/Old Move 序列化并限制包大小 | `CharacterMovementComponent.cpp:9018`、`:9756` | unreliable command packets |
| 服务器重演 | 验证 timestamp 后用 MoveAutonomous 重演输入 | `CharacterMovementComponent.cpp:9840` | authoritative simulation |
| 误差判断 | 位置、模式和基座都可能触发修正 | `CharacterMovementComponent.cpp:10367` 附近 | correction policy |
| 客户端修正 | 应用权威位置、速度、模式与 Base | `CharacterMovementComponent.cpp:11052` | reconciliation state |
| 未确认输入回放 | 从服务器确认点重放剩余 SavedMoves | `CharacterMovementComponent.cpp:8488` | replay unacknowledged commands |
| owner/observer 分路 | owner 纠错；observer 插值 | `Runtime/Engine/Private/Character.cpp:1716` | 两套网络表现管线 |

## 4. Unity 推荐代码结构

如果目标项目已有结构，优先适配现有结构；否则采用：

```text
Assets/GameFramework/
├── Core/
│   ├── IController.cs
│   ├── IPossessable.cs
│   ├── ControlIntent.cs
│   ├── SimulationTick.cs
│   └── GameplayRole.cs
├── Controllers/
│   ├── ControllerBase.cs
│   ├── LocalPlayerController.cs
│   ├── AiController.cs
│   └── PossessionCoordinator.cs
├── PlayerState/
│   ├── PlayerRuntimeState.cs
│   ├── NetworkPlayerState.cs
│   ├── PlayerRegistry.cs
│   └── PlayerStateTransferPolicy.cs
├── Character/
│   ├── CharacterAvatar.cs
│   ├── CharacterMotor.cs
│   ├── CharacterMotorState.cs
│   ├── CharacterMovementConfig.cs
│   ├── MovementMode.cs
│   └── RootMotionAdapter.cs
├── Networking/
│   ├── INetworkGameplayAdapter.cs
│   ├── MoveCommand.cs
│   ├── CharacterSnapshot.cs
│   ├── PredictionBuffer.cs
│   ├── ReconciliationSystem.cs
│   └── SnapshotInterpolator.cs
└── Tests/
    ├── PossessionTests.cs
    ├── PlayerStateLifecycleTests.cs
    ├── CharacterMotorTests.cs
    └── PredictionReplayTests.cs
```

推荐让核心 Motor 与具体网络库解耦。Netcode for GameObjects、Mirror 或 FishNet 只通过 `INetworkGameplayAdapter` 提供 ownership、RPC、NetworkVariable 和 spawn/despawn 能力。

## 5. 开始编码前的项目审计

Agent 必须先完成只读检查，并在首次进度报告中回答：

1. Unity 版本与渲染管线是什么？
2. `Packages/manifest.json` 中使用的是旧 Input Manager 还是 Input System？
3. 是否已经安装 NGO、Mirror、FishNet、Photon 或自研网络层？
4. 是否已有 Player prefab、CharacterController/Rigidbody、自研 Motor、相机系统和 Animator？
5. 是否存在 asmdef、测试程序集、代码风格或 AGENTS.md 约束？
6. `ProjectSettings/TimeManager.asset` 的 Fixed Timestep 是多少？
7. Physics/Physics2D、Layer、碰撞矩阵和 Character layer 如何配置？
8. 目标是单机优先、主机模式还是专用服务器？

审计后记录以下决策：

- Character Motor 使用 Unity `CharacterController`、kinematic Rigidbody，还是自定义 CapsuleCast Motor。
- 网络库及其 owner/server/observer API 映射。
- 模拟 Tick 频率、输入采样频率和快照频率。
- 是否要求严格确定性；若不要求，采用服务器纠错下的近似一致模拟。
- 根运动是否进入首个里程碑。

如果这些选择会明显改变现有项目架构，先报告证据与选项，不要擅自替换已有网络或移动框架。

## 6. 执行计划

每个阶段必须独立可运行、可测试。不要一次性提交整个系统。

### 阶段 0：建立基线

任务：

- 完成项目审计。
- 运行现有编译与测试，记录基线错误。
- 找到或创建最小测试场景，但不得破坏现有场景。
- 确定网络库、输入系统、Motor 方案和代码目录。

交付：

- 一份简短的架构决策记录。
- 现有项目结构与风险清单。
- 可重复执行的编译/测试命令。

验收：

- 修改前项目状态已记录。
- 后续方案不会覆盖现有用户代码或无关变更。

### 阶段 1：核心契约与生命周期

任务：

- 实现 `IController`、`IPossessable`、`ControlIntent`、`GameplayRole`。
- 定义 `PossessionState = None/Granted/Ready`。
- 明确 Controller、PlayerState、Character 的创建与销毁责任。
- 所有关系变化发出 C# event，但事件不得承担权威状态修改。

验收测试：

- Controller 可以占有一个 Character。
- 重复 Possess 同一个 Character 是幂等或给出明确结果。
- 占有新 Character 会完整释放旧 Character。
- 已被其他 Controller 占有的 Character 会按策略先解除旧关系或拒绝。
- Character 销毁后 Controller 不保留悬空引用。

### 阶段 2：本地 PlayerController 与 AIController

任务：

- LocalPlayerController 读取 Unity Input System，将结果写成 `ControlIntent`。
- 独立维护 `ControlRotation`，相机使用它，Character 只选择性跟随 Yaw/Pitch/Roll。
- 输入只在拥有本地玩家的 Controller 上启用。
- AIController 通过相同 `ControlIntent` 驱动 Character，不允许 AI 绕过 Motor 直接写 Transform。

验收测试：

- 换 Character 后只有新 Character 响应输入。
- UI/暂停/观战模式能阻断或切换输入上下文。
- 相机旋转与身体旋转可以分离。
- AI 和本地玩家可以控制同一种 Character prefab。

### 阶段 3：PlayerState 与 PlayerRegistry

任务：

- 实现玩家名、账号 ID、局内 ID、队伍、比分、Ping、观战状态。
- 实现 `PlayerRegistry`，PlayerState 创建时注册、销毁时移除。
- 字段只能通过 server-authoritative mutation API 修改。
- 使用事件通知 UI，不让 PlayerState 直接引用具体 HUD。
- 实现 `PlayerStateTransferPolicy` 白名单迁移。

验收测试：

- Character 死亡和重生不会清除 Score/Team/AccountId。
- 新 Character 能重新关联旧 PlayerState。
- PlayerRegistry 不产生重复项或已销毁项。
- 场景迁移只复制白名单字段。
- Character Transform、MotorState、Animator 和 Buff 不会进入 PlayerState。

### 阶段 4：Character prefab 分层

任务：

- 根节点放置权威胶囊或 CharacterController。
- Visual 子节点放置 Animator 与 SkinnedMeshRenderer。
- CharacterAvatar 只协调 Controller、PlayerState、Motor、生命与表现。
- CharacterMotor 独立保存速度、MovementMode、地面信息和移动基座。

推荐层级：

```text
CharacterAvatar (authority transform + capsule)
├── VisualRoot (network smoothing offset)
│   └── Model / Animator
├── CameraAnchor
└── GameplayComponents
```

验收测试：

- 修改动画或模型比例不会改变权威碰撞。
- VisualRoot 可以平滑偏移，而碰撞根保持服务器/模拟位置。
- CharacterAvatar 不直接读取键盘。

### 阶段 5：离线 CharacterMotor MVP

实现顺序：

1. 固定模拟 Tick。
2. PendingInput 累计与一次性消费。
3. Grounded/Walking 与 Falling。
4. 加速度、最大速度、摩擦和制动。
5. 重力、落地与离地。
6. 胶囊 Sweep、斜坡限制和基础台阶处理。
7. Jump 请求、次数和持有时间。
8. Crouch 胶囊缩放与起立空间检测。
9. MovementMode 切换事件。

验收测试：

- 同样的输入命令序列和初始状态可重复得到足够接近的结果。
- 输入回调频率变化不会改变移动速度。
- Jump 不直接设置 Transform。
- 头顶有障碍时无法起立。
- 斜坡、台阶和落地不会出现明显穿透或无限抖动。

### 阶段 6：服务器权威与观察者快照

任务：

- 服务器生成 Character 权威状态。
- 定义 `CharacterSnapshot`：tick、position、rotation、velocity、movementMode、crouch、baseId/relativePose。
- remote observer 仅做 snapshot buffer、插值、有限外推和 VisualRoot 平滑。
- PlayerState 使用全场可见低频同步；PlayerController 保持 owner-only。

验收测试：

- 两个客户端都能看到对方 PlayerState。
- 远端客户端看不到或不依赖对方 PlayerController。
- 远端 Character 平滑移动，不运行本地输入预测。
- 移动平台使用相对参考系后不会持续漂移。

### 阶段 7：占有网络握手

协议：

```text
Server: GrantPossession(controllerId, characterId)
Owner:  resolve character reference
Owner:  bind local input and camera
Owner:  reset local prediction buffer
Owner → Server: PossessionReady(characterId)
Server: mark acknowledged character
```

要求：

- Grant 和 Ready 使用可靠消息。
- MoveCommand 只有在 Ready 后才发送。
- 引用未解析时等待/重试，不得把 null 当作成功。
- 重复或乱序消息必须幂等。
- 换 Pawn 时旧预测缓冲、输入和相机绑定必须先清理。

### 阶段 8：拥有客户端预测与纠错

先定义纯数据结构：

```csharp
public struct MoveCommand
{
    public uint Tick;
    public Vector2 QuantizedMove;
    public ushort QuantizedYaw;
    public MoveFlags Flags; // Jump, Crouch, Sprint...
}

public struct MotorState
{
    public uint Tick;
    public Vector3 Position;
    public Vector3 Velocity;
    public MovementMode Mode;
    public NetworkObjectId MovementBase;
}
```

实现顺序：

1. 客户端按 tick 生成并量化 MoveCommand。
2. 量化后的命令同时用于本地预测和网络发送。
3. 命令与预测后 MotorState 写入环形缓冲。
4. 服务器验证 tick/包大小/ownership，并用同一 Motor 重演。
5. 服务器发送 ack 或 authority correction。
6. 客户端定位 correction tick，应用权威 MotorState。
7. 按 tick 重放 correction 之后的所有未确认命令。
8. 碰撞根立即纠正；VisualRoot 可在小误差下平滑收敛。

验收测试：

- 0ms、100ms、200ms 延迟下 owner 输入仍立即响应。
- 轻微误差修正不会清除修正之后的输入。
- 丢包后旧的未确认命令能重发或通过后续包恢复。
- 非 owner 无法为该 Character 提交 MoveCommand。
- 篡改 Transform 不会成为服务器权威结果。
- prediction replay 后 MotorMode、velocity 和 movement base 都正确恢复。

### 阶段 9：根运动、移动平台与扩展动作

这一阶段不是首个 MVP 的前置条件。

任务：

- RootMotionAdapter 从 Animator 获取 delta，并转换成 Motor 可消费的 velocity/displacement source。
- 根运动状态进入 MoveCommand/Snapshot，不能只同步最终坐标。
- 移动平台同步 base ID、相对位置、相对旋转和基座速度。
- Sprint、Dash、Knockback、CustomMovement 通过扩展 MoveFlags 和 Motor state 实现。

验收：

- 根运动仍经过胶囊碰撞。
- 平台运动与角色快照频率不一致时不持续漂移。
- 新动作可在预测与服务器重演中得到相同状态转换。

### 阶段 10：集成、性能与回归

任务：

- 添加延迟、抖动、丢包和乱序测试配置。
- 记录每秒 MoveCommand、Snapshot、Correction 数量和字节量。
- 检查对象销毁、重连、场景切换、观战、换 Pawn 和 Host 模式。
- 检查 GC allocation；预测 Tick 中避免 LINQ、装箱和临时集合。
- 为核心状态机、迁移策略和回放算法增加 EditMode tests。
- 为真实 spawn/ownership/RPC 流程增加 PlayMode tests。

## 7. 第一批应实现的最小接口

接口可根据项目风格调整，但责任不能合并回 Character：

```csharp
public interface IController
{
    IPossessable CurrentPawn { get; }
    Quaternion ControlRotation { get; }
    PlayerRuntimeState PlayerState { get; }
    ControlIntent SampleIntent(uint tick);
}

public interface IPossessable
{
    IController Controller { get; }
    void OnPossessed(IController controller);
    void OnUnpossessed(IController previousController);
    void ApplyIntent(in ControlIntent intent, uint tick);
}

public interface ICharacterMotor
{
    MotorState State { get; }
    void Simulate(in MoveCommand command, float deltaTime);
    void Restore(in MotorState state);
}
```

不要让这些接口直接依赖某个具体网络库。具体的 NGO/Mirror/FishNet NetworkBehaviour 应作为适配层包装它们。

## 8. 测试矩阵

| 场景 | 必须验证 |
|---|---|
| 单机出生 | Controller、PlayerState、Character 正确关联 |
| 死亡重生 | 旧 Character 销毁；Controller/PlayerState 保留；新 Character 重新绑定 |
| 换 Pawn/载具 | 旧输入、相机和预测缓冲全部清理 |
| 玩家转 AI | Character/Motor 不变，只替换 Controller 意图源 |
| AI 转玩家 | 同上，并在本地握手完成后启用输入 |
| Host 模式 | server 与 local owner 逻辑不重复执行 |
| 专用服务器 | 不采样设备输入，不创建本地相机 |
| 远端观察者 | 无远端 PlayerController 依赖，PlayerState 与 Character 可见 |
| 延迟/丢包 | owner 预测、server replay、correction/replay 正常 |
| 移动平台 | 相对 Base 状态无持续漂移 |
| 场景切换 | PlayerState 白名单迁移，Character 临时状态不迁移 |
| 掉线重连 | 匹配玩家身份、恢复 PlayerState、重新绑定新 Character |

## 9. 禁止事项

- 不得逐行翻译 `CharacterMovementComponent.cpp` 后一次性提交。
- 不得让 Input System 回调直接 `transform.Translate`。
- 不得把 PlayerState 做成 Character 上随死亡销毁的组件。
- 不得把 PlayerController 广播给所有观察客户端作为玩家资料来源。
- 不得使用 Animator 或 Mesh Transform 作为权威碰撞位置。
- 不得让客户端直接提交并覆盖服务器 Character Transform。
- 不得在 correction 后只瞬移而不重放未确认输入。
- 不得让 owner prediction 与 observer interpolation 共用一套状态缓冲。
- 不得在没有测试的情况下替换目标项目现有网络库、Motor 或输入框架。
- 不得修改 UE 源码；UE 仅作为行为与设计参考。

## 10. Agent 工作方式

每个阶段都必须：

1. 先读取目标文件和现有架构，再修改。
2. 列出准备修改/新增的文件。
3. 只实现当前阶段的最小闭环。
4. 编译并运行相关测试。
5. 报告：完成行为、源码依据、Unity 实现差异、测试结果、已知风险和下一阶段。
6. 保留用户已有修改，不重置或覆盖无关文件。

如果 UE 与 Unity 的能力不完全对应，应复刻外部行为和状态不变量，并在代码注释或设计记录中说明差异。不要为了类名相似而制造无用继承层级。

## 11. 完成定义

只有满足以下条件，三部曲的首个完整版本才算完成：

- Controller 可以稳定 Possess/UnPossess，不产生重复控制或悬空引用。
- PlayerState 能跨 Character 死亡、重生和规定的场景迁移保存玩家级数据。
- Character 使用独立胶囊碰撞根、视觉根和 CharacterMotor。
- 单机移动、跳跃和蹲伏通过固定模拟 Tick 工作。
- 服务器是 Character 和 PlayerState 的最终权威。
- owner client 能预测、接收 correction 并重放未确认输入。
- remote observer 使用快照插值，不执行 owner prediction。
- 自动化测试覆盖控制权、PlayerState 生命周期、Motor 状态机和 prediction replay。
- 目标 Unity 项目可以干净编译，现有功能没有新增回归。

## 12. 建议的首个 Agent 指令

可将下面内容与本文件一起交给项目 Agent：

```text
请先完整阅读 UNITY_UE_GAMEPLAY_REIMPLEMENTATION_AGENT.md。
第一轮只执行“阶段 0：建立基线”：审计当前 Unity 项目，定位已有输入、角色、相机、网络和测试框架，列出证据与架构决策，不修改 gameplay 代码。
审计完成后给出阶段 1 的精确文件级计划；除非现有架构已明确支持，否则不要直接开始网络预测。
后续严格按阶段推进，每个阶段实现、编译、测试并汇报后再进入下一阶段。
```
