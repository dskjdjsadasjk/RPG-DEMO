# 连接层第一版实现说明

## 1. 版本与范围

项目原始版本是 **Unity 6000.2.7f2**。当前机器只有 Unity 2022.3，因此主工程不能在本机完成最终构建；原因是主工程使用了 URP 17.2、UGUI 2.0、Test Framework 1.6 等 Unity 6 包。

当前已经实现连接层和第一层网络对象生命周期：

- 独立 Dedicated Server（DS）进程监听 UDP 端口。
- 客户端连接 DS。
- DS 为每个客户端创建一个 `GameNetConnection`。
- 使用 UTP Reliable Sequenced Pipeline 完成连接握手。
- 握手超时、协议版本校验、Schema 校验、非法状态包拒绝和断开。
- Unity Editor 菜单及命令行构建 DS/Client。
- `NetworkIdentity`、服务端 NetId 分配和客户端对象注册表。
- 每个连接独立的 `ActorReplicationChannel`。
- Reliable `ActorChannelOpen/OpenAck/Close`，用于 Spawn/Despawn。
- DS 登录后创建 `ServerPlayerController`、`PlayerState`，并可选 Spawn/Possess 默认 Pawn。
- Owner 断线后服务端 Despawn 其网络对象，并通知其他 Ready 客户端。

当前没有实现：`ObjectReplicator`、通用 RPC、属性复制、客户端 Possess、本地移动输入、预测、回滚、纠错、平滑和 Replication Graph。

## 2. 类与 UE 对应关系

| 当前实现 | 职责 | UE 对应概念 |
|---|---|---|
| `GameNetDriver` | 管理监听、连接集合、协议分发和握手状态机 | `UNetDriver` |
| `GameNetConnection` | 表示一个客户端到 DS 的逻辑连接及状态 | `UNetConnection` |
| `INetworkTransport` | 隔离上层网络逻辑与底层传输 | `ISocketSubsystem`/底层 Socket 抽象 |
| `UtpTransport` | 封装 UTP Driver、UDP、可靠管线和收发事件 | UE UDP/PacketHandler 的简化组合 |
| `ConnectionProtocol` | 定义连接阶段消息和按字节序列化 | Control Channel 的连接控制消息 |
| `NetworkIdentity` | NetId、PrefabId、Owner、Epoch 和 Role | Actor NetGUID/Role/Owner |
| `NetworkObjectRegistry` | 当前进程的 NetId → GameObject 映射 | GuidCache/Network Object Map |
| `NetworkPrefabRegistry` | PrefabId → Prefab 的固定映射 | 服务端/客户端共享 Class/Archetype 映射 |
| `ActorReplicationChannel` | 每个 Connection × Actor 一份的生成确认状态 | `UActorChannel` 的最小子集 |
| `ServerGameMode` | Ready、Login、Spawn Pawn、Possess、Logout | `AGameModeBase` 登录生命周期 |
| `NetworkBootstrap` | 根据构建类型和启动参数创建 DS 或 Client | Engine/GameInstance 启动网络驱动 |
| `NetworkBuild` | 生成独立 DS 和 Client 可执行文件 | Server/Client Target 构建配置 |

ActorChannel 只有连接进入 `Ready` 后才会创建。普通状态和 RPC 尚未实现，并且后续必须等待 `SpawnAcked` 后才能发送。

## 3. 连接与握手流程

```text
Client                         Dedicated Server
  |---- UTP Connect ----------------->|
  |---- ClientHello ----------------->|  协议版本、SchemaHash、ClientNonce
  |<--- ServerChallenge --------------|  回显 ClientNonce、ServerNonce
  |---- ClientLogin ----------------->|  ServerNonce、DisplayName
  |<--- ServerWelcome ----------------|  ConnectionId、ServerTick、TickRate
  |---- ClientReady ----------------->|
  |          双方进入 Ready            |
```

握手消息一包对应一个 UTP 数据报，全部走 Reliable Sequenced Pipeline。第一版协议使用小端整数：

| 消息 | 字段 |
|---|---|
| `ClientHello` | Type:u8, ProtocolVersion:u16, SchemaHash:u32, ClientNonce:u64 |
| `ServerChallenge` | Type:u8, ProtocolVersion:u16, ClientNonce:u64, ServerNonce:u64 |
| `ClientLogin` | Type:u8, ServerNonce:u64, NameLength:u8, Name:utf8 |
| `ServerWelcome` | Type:u8, ConnectionId:u32, ServerTick:u32, TickRate:u16 |
| `ClientReady` | Type:u8, ConnectionId:u32 |
| `ServerReject` | Type:u8, RejectReason:u8, MessageLength:u8, Message:utf8 |

当前 Challenge 只用于验证握手顺序和阻止旧包误用，不等同于账号认证或加密。正式登录需要在后续阶段接入 Token/Session 验证。

## 4. 启动参数

DS 构建带有 `UNITY_SERVER`，不传参数也会自动以服务器模式启动：

```powershell
.\Builds\Server\RPGDemoServer.exe -batchmode -nographics -port 7777 -logFile Logs\server.log
```

客户端只有传入 `-connect` 才会自动启动网络：

```powershell
.\Builds\Client\RPGDemoClient.exe -connect 127.0.0.1:7777 -name ClientA -logFile Logs\client-a.log
```

支持的业务参数：

- `-server` 或 `-dedicatedServer`：服务器模式。
- `-client`：客户端模式；还需要地址，通常直接使用 `-connect`。
- `-connect 127.0.0.1:7777`：客户端服务器地址。
- `-connect [::1]:7777`：IPv6 地址。
- `-port 7777`：覆盖默认 UDP 端口。
- `-name ClientA`：客户端显示名，协议最多传输 32 字节 UTF-8。
- `-playerPrefab 1`：DS 默认玩家 Pawn 的 PrefabId；场景 Bootstrap 已配置时可以不传。
- `-maxPlayers 16`：DS 最大登录玩家数。

第一版 `UtpTransport` 只接受数字 IPv4/IPv6 地址，不做 DNS 域名解析。

## 5. 构建方式

安装 Unity 6000.2.7f2 和 Windows Dedicated Server Build Support 后，可在 Editor 使用：

- `RPG Demo/Build/Dedicated Server (Windows)`
- `RPG Demo/Build/Client (Windows)`
- `RPG Demo/Build/Server + Client (Windows)`

命令行构建：

```powershell
Unity.exe -batchmode -quit -projectPath E:\RPG-DEMO `
  -executeMethod RPGDemo.GameFramework.Editor.NetworkBuild.BuildDedicatedServer `
  -logFile E:\RPG-DEMO\Logs\build-server.log

Unity.exe -batchmode -quit -projectPath E:\RPG-DEMO `
  -executeMethod RPGDemo.GameFramework.Editor.NetworkBuild.BuildClient `
  -logFile E:\RPG-DEMO\Logs\build-client.log
```

输出路径：

- `Builds/Server/RPGDemoServer.exe`
- `Builds/Client/RPGDemoClient.exe`

## 6. 成功日志

正确联通后，服务端应出现：

```text
[Net][DS] Listening on UDP 0.0.0.0:7777
[Net][DS] Transport accepted ...; awaiting ClientHello
[Net][DS] ClientHello accepted ...; challenge sent
[Net][DS] Login accepted ...; Welcome sent
[Net][DS] READY connection=1, player='ClientA' ...
```

客户端应出现：

```text
[Net][Client] Connecting to 127.0.0.1:7777 as 'ClientA'
[Net][Client] Transport connected; ClientHello sent
[Net][Client] ServerChallenge accepted; ClientLogin sent
[Net][Client] READY connection=1, serverTick=..., tickRate=60
```

## 7. Unity 6 最终验收顺序

1. 用 Unity 6000.2.7f2 打开工程，确认 Console 无编译错误。
2. 确认 Package Manager 已安装 `com.unity.transport 2.4.0`。
3. 构建 DS 和 Client。
4. 启动一个 DS，确认 UDP 7777 正在监听。
5. 启动一个客户端，确认双方都进入 `Ready`。
6. 再启动第二个客户端，确认 DS 分配不同的 `ConnectionId`，并同时保留两个 `GameNetConnection`。
7. 关闭一个客户端，确认 DS 只移除对应连接且继续监听。
8. 使用错误协议版本/Schema 的客户端验证 `ServerReject` 和断开流程。

完成连接验收后，继续验证下面的 NetObjectId、Spawn/Ack/Despawn；两层都稳定后再进入属性复制，不先接移动同步。

## 8. Network Prefab 与 Spawn 配置

代码已具备 Spawn/Despawn，但工程当前没有可直接使用的 Character Prefab，因此需要在 Unity 6000.2.7f2 中完成一次资源配置：

1. Character Prefab 根节点添加 `NetworkIdentity`。
2. 将 `NetworkIdentity.PrefabId` 设置为非零固定值，例如 `1`。
3. 创建 `RPG Demo/Networking/Prefab Registry` 资产。
4. Registry 中添加 `PrefabId=1 → Character Prefab`，两处 PrefabId 必须一致。
5. 将资产保存为 `Assets/Resources/NetworkPrefabRegistry.asset`，或赋给场景中的 `NetworkBootstrap`。
6. 将 Bootstrap 的 `Default Player Prefab Id` 设为 `1`；也可通过 DS 参数 `-playerPrefab 1` 指定。
7. 场景中放置若干 `NetworkPlayerStart`，通过 `Spawn Order` 固定顺序。

生命周期：

```text
连接 Ready
→ ServerGameMode 创建 ServerPlayerController + PlayerState
→ DS Instantiate Pawn 并分配 NetId/Owner/Epoch
→ 每个 Ready Connection 创建独立 ActorReplicationChannel
→ ActorChannelOpen（Reliable）
→ Client 按 PrefabId Instantiate，注册 NetId，确定 Autonomous/Simulated Role
→ ActorChannelOpenAck（Reliable）
→ Server Channel.SpawnAcked=true
```

断线时，DS 只销毁断线连接拥有的对象；其他连接上的对应 ActorChannel 收到 `ActorChannelClose` 后销毁客户端副本。禁止直接 `Destroy` 已 Spawn 的权威对象，应调用 `GameNetDriver.DespawnServerObject`。

下一步是实现最小 `ObjectReplicator` 和一个低频属性证明；完成后再实现客户端本地 PlayerController/Possess。
