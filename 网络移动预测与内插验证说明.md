# 网络移动预测与内插验证说明

## 已实现链路

当前版本已接通第一版服务器权威移动：

```text
AutonomousProxy 读取本地输入并立即移动
→ 保存 SavedMove
→ Unreliable ClientMove
→ DS 校验 Owner、AuthorityEpoch、Sequence、DeltaTime 和输入长度
→ DS 使用 CharacterMovementComponent 权威模拟
→ Unreliable ServerMoveAck
→ Owner 删除已确认 Move；误差超过 5 cm 时回到权威状态并重放未确认 Move
→ DS 以 20 Hz 向其他客户端发送 TransformSnapshot
→ SimulatedProxy 使用约 100 ms 的快照缓冲进行位置 Lerp 和旋转 Slerp
```

协议位于：

- `Assets/GameFramework/Runtime/Networking/Protocol/CharacterMovementProtocol.cs`
- `ClientMove`：NetId、AuthorityEpoch、MoveSequence、ClientTick、DeltaTime、世界输入方向、ControlYaw。
- `ServerMoveAck`：已确认 Sequence、ServerTick、权威位置、旋转、速度和 MovementMode。
- `TransformSnapshot`：ServerTick、位置、旋转、速度和 MovementMode。

核心实现位于：

- `Assets/GameFramework/Runtime/Networking/Replication/CharacterNetworkMovement.cs`
- `Assets/GameFramework/Runtime/Movement/CharacterMovementComponent.cs`
- `Assets/GameFramework/Runtime/Networking/GameNetDriver.cs`

角色分工：

- `Authority`：关闭普通自动移动，只处理客户端发来的 Move。
- `AutonomousProxy`：普通移动组件本地预测，并维护 SavedMove 队列。
- `SimulatedProxy`：关闭普通自动移动，只由服务器快照内插驱动。

## 重新生成和构建

Unity 完成脚本编译后，执行：

```text
RPG Demo → Networking → Create or Refresh Network Test Content
```

生成的 `NetworkPlayer.prefab` 应包含 `CharacterNetworkMovement`。

然后重新构建：

```text
RPG Demo → Build → Client (Windows)
```

必须重新构建；旧的 `RPGDemoClient.exe` 不包含移动协议，新旧程序的 SchemaHash 也不一致。

## 启动

先停止旧服务端，再启动新 DS：

```powershell
New-Item -ItemType Directory -Force 'E:\RPG-DEMO\Logs' | Out-Null

$server = Start-Process `
  -FilePath 'E:\RPG-DEMO\Builds\Client\RPGDemoClient.exe' `
  -ArgumentList @(
    '-server', '-batchmode', '-nographics',
    '-port', '7777',
    '-playerPrefab', '1',
    '-logFile', 'E:\RPG-DEMO\Logs\server.log'
  ) `
  -WindowStyle Hidden `
  -PassThru
```

客户端 A：

```powershell
Start-Process `
  -FilePath 'E:\RPG-DEMO\Builds\Client\RPGDemoClient.exe' `
  -ArgumentList @(
    '-connect', '127.0.0.1:7777',
    '-name', 'ClientA',
    '-logFile', 'E:\RPG-DEMO\Logs\client-a.log'
  )
```

客户端 B：

```powershell
Start-Process `
  -FilePath 'E:\RPG-DEMO\Builds\Client\RPGDemoClient.exe' `
  -ArgumentList @(
    '-connect', '127.0.0.1:7777',
    '-name', 'ClientB',
    '-logFile', 'E:\RPG-DEMO\Logs\client-b.log'
  )
```

## 验收

点击客户端 A 的窗口后按 WASD。左上角 HUD 应满足：

- 自己的 `AutonomousProxy` 位置立即变化。
- `Ack` 持续增加，`Pending` 保持在较小范围。
- `Corrections` 只在预测与 DS 产生明显误差时增加。
- 客户端 B 中对应的 `SimulatedProxy` 平滑移动，`Snapshots` 通常保持至少 1 个。

关键日志：

```text
[Input] PlayerInput activated; map=Player, MoveEnabled=True.
[Net][Move][Client] Local movement input active ...
[Net][Move][DS] Processing ClientMove ...
[Net][Move][DS] First non-zero move ...
[Net][Move][Client] Prediction acknowledged ...
[Net][Move][Client] Snapshot interpolation active ...
```

筛选命令：

```powershell
Get-Content 'E:\RPG-DEMO\Logs\server.log' -Tail 200 |
  Select-String '\[Net\]\[Move\]'

Get-Content 'E:\RPG-DEMO\Logs\client-a.log' -Tail 200 |
  Select-String '\[Input\]|\[Net\]\[Move\]'
```

客户端窗口未获得焦点时不会读取物理键盘，这是正常现象。

## 第一版暂未实现

- Move 批处理和冗余发送。
- FVector_NetQuantize 风格的位置、速度和输入量化。
- 网络丢包/抖动下的动态内插延迟。
- 短时间外推和外推超时。
- 视觉 Mesh 与碰撞胶囊分离后的 Owner 纠错平滑。
- 跳跃、下落、根运动、移动模式切换和复杂平台运动。

这些应在基础走路链路验证通过后继续增加。
