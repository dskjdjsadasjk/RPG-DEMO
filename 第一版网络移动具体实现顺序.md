# RPG-DEMO：第一版网络移动具体实现顺序

> 已确定第一版只采用独立 Dedicated Server。DS 进程、登录生命周期、NetDriver/Connection/ActorChannel/ObjectReplicator、服务端与客户端执行顺序的定稿见 [DS第一版网络实现规划.md](DS第一版网络实现规划.md)。

## 1. 第一版最终要跑出什么

第一版只完成一条可玩的纵向链路：

```text
一个 Dedicated Server
→ 两个 Client 连接
→ 服务端为每个连接创建一个 Character
→ 每个 Client 立即预测自己的 Walking
→ 服务端按相同移动代码权威模拟
→ 拥有者收到 Ack 或位置纠正并重放未确认输入
→ 另一个 Client 通过服务端快照看到该 Character 平滑移动
```

第一版支持：

- 连接、断开、握手。
- 服务端分配 ConnectionId 和 NetId。
- 服务端 Spawn Character、建立所有权、Possess。
- `Authority`、`AutonomousProxy`、`SimulatedProxy`。
- Walking、平面输入、Yaw、静态墙体碰撞。
- 本地预测、服务端权威模拟、累计 Ack、Correction、Replay。
- 远端 Character 的 20 Hz 快照和插值。
- 100 ms RTT、5% 丢包下继续移动并最终收敛。

第一版不实现：

- 通用属性复制系统。
- 通用 RPC 反射框架。
- Interest Grid、Dormancy、Replication Graph/Iris。
- Jump/Falling、移动平台、Root Motion、技能预测。
- 位级压缩和 UE 完整 Packed Vector。
- Lobby、账号、匹配、Relay、加密。

第一版完成标准不是“网络模块目录建好了”，而是两个独立客户端可以在服务端权威下互相看到对方移动。

---

## 2. 先后顺序

正确实施顺序是：

```text
1. Transport 能连接
2. Hello/Welcome 握手成功
3. 服务端 Spawn，客户端创建网络角色
4. 现有 Walking 改为固定 Tick、可重复调用
5. 客户端产生 Move、立即预测并发给服务端
6. 服务端权威执行、返回 Ack/Correction
7. 客户端纠错并 Replay
8. 服务端给其他客户端发 Snapshot，远端插值
9. 断线清理和延迟/丢包联调
```

测试不是第一项功能。每一步代码实际跑通后，再补该步骤最必要的验收测试，避免后续修改破坏已工作的链路。

---

## 3. 第一版技术选择

### 3.1 Transport

使用 Unity Transport 作为底层，不使用 NGO 的 `NetworkTransform` 和角色移动同步。

Unity Transport 负责：

- UDP Socket。
- Connect/Accept/Disconnect。
- Reliable Sequenced Pipeline。
- Unreliable Pipeline。
- 底层数据收发。

本项目自己负责：

- 连接状态和握手协议。
- NetId、所有权和网络角色。
- Spawn/Possess 消息。
- Character Move、预测历史和服务端权威模拟。
- Ack/Correction/Replay。
- Snapshot 和插值。

第一版直接使用 Transport 的 `DataStreamWriter/DataStreamReader` 写固定字段，不先实现 `NetBitWriter`。等链路正确、能统计真实带宽后，再做位压缩。

### 3.2 网络频率

```text
Character Simulation = 60 Hz
Client Move Send      = 30 Hz
Server Snapshot       = 20 Hz
Remote Interpolation  = 100 ms 延迟
```

这些值统一放入 `NetworkSettings`，客户端和服务端握手时校验 Simulation Tick Rate。

---

## 4. 第一版真正需要的类

不要一开始创建完整 UE 类族。第一版只需要下面这些类：

```text
Assets/GameFramework/Runtime/Networking/
├── Bootstrap/
│   └── NetworkBootstrap.cs
├── Transport/
│   ├── INetworkTransport.cs
│   └── UnityTransportAdapter.cs
├── Core/
│   ├── GameNetworkDriver.cs
│   ├── NetworkSettings.cs
│   ├── NetworkSimulationLoop.cs
│   ├── NetworkRole.cs
│   └── NetworkMode.cs
├── Protocol/
│   ├── NetworkMessageType.cs
│   ├── ConnectionMessages.cs
│   ├── LifecycleMessages.cs
│   ├── CharacterMoveMessages.cs
│   └── CharacterSnapshotMessages.cs
├── Server/
│   ├── ServerSession.cs
│   ├── ServerConnectionContext.cs
│   ├── ServerNetworkWorld.cs
│   ├── ServerPlayerSpawner.cs
│   └── ServerCharacterMoveProcessor.cs
├── Client/
│   ├── ClientSession.cs
│   ├── ClientNetworkWorld.cs
│   ├── ClientCharacterPrediction.cs
│   └── RemoteCharacterInterpolator.cs
├── Identity/
│   ├── NetworkIdentity.cs
│   └── NetworkPrefabRegistry.cs
└── Movement/
    ├── CharacterMoveCommand.cs
    ├── CharacterMovementState.cs
    ├── SavedCharacterMove.cs
    ├── CharacterPredictionBuffer.cs
    ├── ServerMoveQueue.cs
    └── CharacterSnapshotBuffer.cs
```

现有移动代码中再新增：

```text
Assets/GameFramework/Runtime/Movement/
├── CharacterMovementSimulation.cs
└── CharacterMovementSettings.cs
```

第一版不创建：

- `ActorChannel` 的完整复刻。
- 任意属性 `ReplicationDescriptor`。
- 通用 Reliable RPC Generator。
- Iris Fragment/Bridge。
- Interest Manager。

---

## 5. 第一版协议

第一版协议只有七种消息。

### 5.1 消息和可靠性

| 消息 | 方向 | Pipeline |
|---|---|---|
| `Hello` | Client → Server | Reliable Sequenced |
| `Welcome` | Server → Client | Reliable Sequenced |
| `SpawnCharacter` | Server → Client | Reliable Sequenced |
| `DespawnCharacter` | Server → Client | Reliable Sequenced |
| `ClientMoveBatch` | Client → Server | Unreliable |
| `CharacterMoveResponse` | Server → Owner | Unreliable，重复直到回执 |
| `CharacterSnapshot` | Server → Other Clients | Unreliable |

每个消息第一个字段都是：

```text
MessageType : byte
```

第一版不额外实现通用 Packet Header、Ack Bitfield 或多消息聚合器。Transport 已经处理底层包，Character 协议只维护自己的 Tick、BatchSequence 和 ResponseSequence。

### 5.2 `Hello`

```text
MessageType       : byte
ProtocolVersion   : ushort
ClientNonce       : ulong
```

服务端只接受支持的版本。失败时主动断开。

### 5.3 `Welcome`

```text
MessageType       : byte
ProtocolVersion   : ushort
ConnectionId      : uint
ServerTick        : uint
SimulationTickRate: ushort
```

客户端收到后从 `Connecting` 进入 `Connected`，记录自己的 ConnectionId，并开始估算 ServerTick。

### 5.4 `SpawnCharacter`

```text
MessageType       : byte
NetId             : uint
OwnerConnectionId : uint
AuthorityEpoch    : ushort
PrefabId          : ushort
Position          : float × 3
Yaw               : ushort
```

客户端判断：

```text
OwnerConnectionId == LocalConnectionId
→ AutonomousProxy，启用本地输入和预测

否则
→ SimulatedProxy，禁用输入，启用 Snapshot 插值
```

服务端实例永远是 `Authority`。

### 5.5 `ClientMoveBatch`

```text
MessageType             : byte
NetId                   : uint
AuthorityEpoch          : ushort
BatchSequence           : ushort
LastAppliedResponseSeq  : ushort
MoveCount               : byte，1..4

每个 Move：
ClientTick              : uint
InputX                  : sbyte
InputY                  : sbyte
ControlYaw              : ushort
MoveFlags               : byte

Batch 末尾，仅描述最新 Move 的预测结果：
NewestClientEndPosition : float × 3
NewestMovementMode      : byte
```

30 Hz 发包时通常包含：

- 本次新产生的两个 60 Hz Move。
- 最近一个冗余 Move。
- 最老未确认 Move，用来修复突发丢包造成的 Tick 缺口。

同一 Tick 在一个 Batch 内只能出现一次。

### 5.6 `CharacterMoveResponse`

固定部分：

```text
MessageType       : byte
NetId             : uint
AuthorityEpoch    : ushort
ResponseSequence  : ushort
AckClientTick     : uint
ServerTick        : uint
Flags             : byte
```

`Flags.IsCorrection == false` 时，到此结束。

纠正部分：

```text
Position          : float × 3
Velocity          : float × 3
Yaw               : ushort
MovementMode      : byte
```

服务端保留未回执的 Correction，并在后续响应中重复。客户端通过下一次 `ClientMoveBatch.LastAppliedResponseSeq` 回执。

### 5.7 `CharacterSnapshot`

```text
MessageType       : byte
SnapshotSequence  : ushort
ServerTick        : uint
EntityCount       : ushort

每个 Entity：
NetId             : uint
AuthorityEpoch    : ushort
Position          : float × 3
Velocity          : float × 3
Yaw               : ushort
MovementMode      : byte
Flags             : byte
```

第一版使用 float 是为了先确认数据流和误差来源。量化是第一版跑通后的第一个带宽优化：Position 改厘米定点，Velocity 改厘米/秒，Yaw 保持 ushort。

---

## 6. 第一步：先实现服务器和客户端连接

### 6.1 新增 `NetworkBootstrap`

职责：

- Inspector 中选择 `Standalone/Server/Client/Host`。
- 读取 Address、Port。
- Dedicated Server Build 可从命令行读取 `-server -port=7777`。
- 创建一个 `GameNetworkDriver`。
- 不负责 Spawn、移动或协议解析。

调用方式：

```text
StartServer
→ UnityTransportAdapter.Bind(port)
→ Listen
→ ServerSession 创建

StartClient
→ UnityTransportAdapter.Connect(address, port)
→ ClientSession 创建
```

### 6.2 实现 `UnityTransportAdapter`

只封装以下接口：

```csharp
StartServer(ushort port)
StartClient(string address, ushort port)
PollEvents()
SendReliable(connectionId, writer)
SendUnreliable(connectionId, writer)
Disconnect(connectionId)
Dispose()
```

不要让上层代码直接依赖 Unity Transport 的 Connection 结构。`connectionId` 由 `GameNetworkDriver` 映射。

### 6.3 实现握手

服务端流程：

```text
Transport Accept
→ 创建 ServerConnectionContext(State=Handshaking)
→ 等 Hello
→ 校验 ProtocolVersion
→ 分配 ConnectionId
→ 发 Welcome
→ State=Connected
```

客户端流程：

```text
Transport Connected
→ 发 Hello
→ 等 Welcome
→ 保存 ConnectionId/ServerTick/TickRate
→ State=Connected
```

这一步完成标准：

- 可以分别启动一个 Server Build 和一个 Client Build。
- Server Console 打印 Accept、Hello、ConnectionId。
- Client Console 打印 Welcome 和 ServerTick。
- 关闭 Client 后 Server 能收到 Disconnect 并清理连接。

此时还没有 Character，也没有移动。

---

## 7. 第二步：服务端 Spawn，客户端创建角色

### 7.1 `NetworkIdentity`

字段：

```csharp
uint NetId;
ushort AuthorityEpoch;
uint OwnerConnectionId;
NetworkRole Role;
ushort PrefabId;
```

规则：

- NetId 只能由服务端分配。
- 客户端不能修改 OwnerConnectionId。
- 重生或所有权重新建立时 AuthorityEpoch 增加。
- 旧 Epoch 的 Move、Response、Snapshot 全部丢弃。

### 7.2 服务端 Spawn

`ServerPlayerSpawner` 在连接进入 `Connected` 后：

1. 从 `NetworkPrefabRegistry` 取 Character Prefab。
2. 服务端 Instantiate。
3. 分配 NetId。
4. 设置 Role=`Authority`、OwnerConnectionId、AuthorityEpoch=1。
5. 创建或关联当前 `PlayerController`。
6. 服务端调用 `Possess(character)`。
7. 把实体注册进 `ServerNetworkWorld`。
8. 向所有已连接客户端可靠发送 `SpawnCharacter`。
9. 新客户端连接时，补发当前所有已存在 Character 的 Spawn。

### 7.3 客户端 Spawn

`ClientNetworkWorld.HandleSpawnCharacter`：

1. 检查 NetId 是否已存在，重复 Spawn 必须幂等。
2. 根据 PrefabId 创建 Prefab。
3. 写入 NetworkIdentity。
4. 如果 OwnerConnectionId 等于本地 ConnectionId：
   - Role=`AutonomousProxy`。
   - 创建/启用本地 PlayerController 和 InputComponent。
   - 本地 Possess，但不能改服务端所有权。
5. 否则 Role=`SimulatedProxy`：
   - 禁用玩家输入。
   - 禁止 `CharacterMovementComponent` 自己消费输入移动。
   - 启用 `RemoteCharacterInterpolator`。

这一步完成标准：

- 两个客户端连接后，服务端有两个 Authority Character。
- 每个客户端场景中都有两个 Character。
- 每个客户端只有自己的 Character 是 AutonomousProxy。
- 尚未发送移动，三个实例的位置相同。

---

## 8. 第三步：把现有 Walking 改成固定 Tick

这一步才开始修改移动代码。

### 8.1 修改 `PawnMovementComponent`

停止让它在自己的 `Update()` 中调用：

```csharp
TickComponent(Time.deltaTime);
```

改为由 `NetworkSimulationLoop` 每 1/60 秒调用。Standalone 也走同一条固定 Tick 路径。

### 8.2 新建命令和状态

```csharp
public struct CharacterMoveCommand
{
    public uint ClientTick;
    public sbyte InputX;
    public sbyte InputY;
    public ushort ControlYaw;
    public byte MoveFlags;
}

public struct CharacterMovementState
{
    public Vector3 Position;
    public Quaternion Rotation;
    public Vector3 Velocity;
    public MovementMode MovementMode;
}
```

### 8.3 唯一移动入口

从现有 `CharacterMovementComponent` 抽出：

```csharp
SimulateMove(
    in CharacterMoveCommand command,
    float fixedDeltaTime);
```

该方法内部继续复用现有：

- `ConstrainInputAcceleration`。
- `ScaleInputAcceleration`。
- `CalcVelocity`。
- `ApplyVelocityBraking`。
- `PhysWalking`。
- `CharacterController.Move`。

但它不能读取：

- `Time.deltaTime`。
- Unity InputSystem。
- 网络连接。
- 随机数。

再实现：

```csharp
CaptureState()
RestoreState(in CharacterMovementState state)
```

这一步完成标准：

- Standalone 模式仍能正常 Walking。
- 30/60/144 FPS 下，执行同样数量的 Simulation Tick 后位置基本一致。
- Capture → 移动 → Restore → 重放可以回到同样结果。

这里只为已经实现的移动逻辑补少量验证，不先建设庞大的测试框架。

---

## 9. 第四步：客户端预测并发送 Move

只在 `AutonomousProxy` 上执行。

每个 60 Hz Simulation Tick：

```text
读取 PlayerInputAccumulator
→ 构造 CharacterMoveCommand(ClientTick=N)
→ Capture StartState
→ 本地 SimulateMove，玩家立即看到移动
→ Capture EndState
→ 保存 SavedCharacterMove
```

`CharacterPredictionBuffer` 是固定容量 256 的环形缓冲：

```text
ClientTick
Command
StartState
EndState
```

每 2 个 Simulation Tick，即 30 Hz：

1. 取这两次新 Move。
2. 加最近一个冗余 Move。
3. 加最老未确认 Move。
4. 去重后写 `ClientMoveBatch`。
5. 用 Unreliable Pipeline 发送。

此时服务端可以先只打印收到的 NetId、ClientTick、Input，不执行移动。

这一步完成标准：

- 本地 Character 不等待网络即可移动。
- Server 能持续收到单调的 ClientTick。
- 5% 丢包时，通过冗余仍能收到缺失 Tick。
- SimulatedProxy 仍不自行移动。

---

## 10. 第五步：服务端权威执行 Move

`ServerCharacterMoveProcessor` 收到 Batch：

1. 用发送连接查 OwnerConnectionId，禁止控制其他人的 NetId。
2. 检查 AuthorityEpoch。
3. 检查 MoveCount 范围和输入值。
4. 按 ClientTick 去重，写入固定容量 `ServerMoveQueue`。
5. 只从 `LastProcessedClientTick + 1` 开始连续取 Move。
6. 对每个连续 Move 调用与客户端相同的 `SimulateMove`。
7. 限制单个服务端 Tick 最大补跑数，客户端不能靠一次发送大量未来 Tick 加速。
8. 更新 `LastProcessedClientTick`。
9. 比较服务端结果与 Batch 中最新客户端 EndPosition。

服务端不能使用客户端传来的：

- DeltaTime。
- Velocity。
- MaxSpeed。
- MaxAcceleration。
- 最终 Transform。

服务端只信任经过范围校验的输入意图，Movement Settings 使用服务端配置。

这一步完成标准：

- 服务端 Character 跟随客户端输入移动。
- 重复的冗余 Move 不会执行两次。
- 非 Owner 发来的 Move 被拒绝。
- 旧 Epoch 和未来 Tick 被拒绝。

---

## 11. 第六步：Ack、Correction 和 Replay

### 11.1 服务端返回

如果服务端与客户端报告的位置误差小于容差：

```text
发 Ack：AckClientTick = LastProcessedClientTick
```

如果位置或 MovementMode 不一致：

```text
发 Correction：
AckClientTick
Server Position
Server Velocity
Server Yaw
Server MovementMode
```

Correction 保存到 `ServerCharacterPredictionData.PendingResponse`。客户端没有通过 `LastAppliedResponseSeq` 回执前，服务端重复发送，不能被后续普通 Ack 覆盖。

### 11.2 客户端处理 Ack

```text
删除 PredictionBuffer 中所有 Tick <= AckClientTick 的 Move
更新 LastAckedClientTick
```

Tick 比较必须通过回绕安全函数，不能直接使用普通 `<`。

### 11.3 客户端处理 Correction

严格顺序：

```text
找到 AckClientTick 对应的 SavedMove
→ 保存纠正前的 VisualRoot 世界姿态
→ 把逻辑 Character Restore 到服务端 Position/Velocity/Yaw/Mode
→ 删除所有 <= AckClientTick 的 SavedMove
→ 按 ClientTick 重放剩余未确认 Move
→ 得到新的逻辑最终位置
→ 计算 VisualRoot Offset，逐帧衰减到 0
→ 回执 ResponseSequence
```

必须立即纠正逻辑 Capsule，不能对 `CharacterController.transform` 慢慢 Lerp。只有模型所在的 `VisualRoot` 做平滑。

如果找不到 AckClientTick，说明历史已溢出或断档：

```text
直接吸附到服务端状态
→ 清空预测历史
→ 重新从下一个 ClientTick 开始
```

这一步完成标准：

- 正常网络下 Ack 持续推进，Pending Move 数保持稳定。
- 手工篡改客户端位置后，服务端发 Correction。
- Correction 后未确认输入被重放，不会出现输入丢失。
- 丢失一次 Correction 后，重复响应仍能恢复。

---

## 12. 第七步：其他客户端看到远端移动

服务端每 50 ms，即 20 Hz，遍历 Authority Character：

```text
为 Client A 构建 Snapshot 时：
- 不发送 A 自己拥有的 Character Transform
- 发送 B 的 Character State

为 Client B 构建 Snapshot 时：
- 不发送 B 自己拥有的 Character Transform
- 发送 A 的 Character State
```

`RemoteCharacterInterpolator` 收到 Snapshot：

1. 校验 NetId、Epoch、SnapshotSequence。
2. 按 ServerTick 放入固定容量 Buffer。
3. 渲染目标时间为 `EstimatedServerTick - 6 Tick`，即约 100 ms。
4. 找到目标两侧 Snapshot。
5. Position 用 Lerp，Rotation 用 Slerp。
6. Snapshot 不足时最多外推 100 ms，之后保持最后状态。
7. Epoch 变化、Teleport 或距离过大时清 Buffer 并吸附。

远端 SimulatedProxy：

- 不读取输入。
- 不发送 Move。
- 不运行拥有者预测。
- 不参与 Correction Replay。
- 只由 Snapshot Buffer 驱动显示。

这一步完成标准：

- Client A 能看到 Client B 平滑移动，反之亦然。
- 20 Hz Snapshot 在 60/144 FPS 下不会明显一格一格跳。
- 乱序 Snapshot 不会使角色倒退。

---

## 13. 第八步：断线、重生和网络恶化

连接断开时服务端：

```text
停止接收该连接 Move
→ UnPossess
→ 从 ServerNetworkWorld 注销 Character
→ 向其他客户端可靠发 DespawnCharacter
→ Destroy Character/Controller/ConnectionContext
```

客户端处理 Despawn：

```text
从 ClientNetworkWorld 注销 NetId
→ 清 PredictionBuffer/SnapshotBuffer
→ 解除 Controller
→ Destroy GameObject
```

然后加入网络模拟：

- 50/100/200 ms RTT。
- 10/20/50 ms Jitter。
- 1/5/10% Loss。
- 乱序和重复包。

这里只需要一个简单 Debug HUD：

```text
ConnectionState
LocalTick / EstimatedServerTick
LastSentMove / LastAckMove
PendingMoveCount
RTT / Loss
CorrectionCount / LastError
SnapshotBufferCount
BytesPerSecond
```

---

## 14. 测试应该什么时候写

不是先写一套空测试框架，而是每完成一段真实逻辑，立即锁住最危险的行为：

| 已完成的功能 | 此时补的测试 |
|---|---|
| Hello/Welcome | 协议版本不匹配、重复 Hello、断线清理 |
| NetId/Spawn | 重复 Spawn 幂等、旧 Epoch 丢弃 |
| 固定 Tick Walking | 相同命令重放、Capture/Restore |
| Prediction Buffer | Ack 删除、回绕、满容量 |
| ServerMoveQueue | 乱序、重复、缺口补齐、未来 Tick |
| Correction | 0/1/N 个未确认 Move 重放 |
| Snapshot | 乱序、插值、外推上限、Teleport |

因此，第一项代码是 Transport 连接，不是测试；但连接跑通后就补两个最小握手测试，避免以后协议修改把连接弄坏。

---

## 15. 建议的提交顺序

每个提交都应能运行：

1. `network: add Unity Transport bootstrap and server/client connect`
2. `network: add hello welcome handshake and connection ids`
3. `network: add network identity and server-authoritative character spawn`
4. `movement: move walking simulation to fixed network tick`
5. `network: add autonomous client move prediction and batching`
6. `network: add authoritative server move processing`
7. `network: add move ack correction and client replay`
8. `network: add simulated proxy snapshots and interpolation`
9. `network: add disconnect cleanup and network emulation metrics`

只有第 1、2 个提交完成后，才开始谈服务器和客户端角色；只有第 4 个提交完成后，才开始接预测；只有 Owner 闭环正确后，才实现远端 Snapshot。

---

## 16. 与 UE 的直接对应

| 第一版步骤 | 本项目 | UE 对应 |
|---|---|---|
| 连接轮询 | `GameNetworkDriver` | `UNetDriver::TickDispatch/TickFlush` |
| 连接状态 | `ServerConnectionContext/ClientSession` | `UNetConnection` |
| 实体生命周期 | `ServerNetworkWorld/ClientNetworkWorld/NetworkIdentity` | `ActorChannel/NetGUID/Role` |
| 本地预测 | `ClientCharacterPrediction` | `ReplicateMoveToServer` |
| 未确认历史 | `CharacterPredictionBuffer` | `FSavedMove_Character` / Client Prediction Data |
| Move 协议 | `ClientMoveBatch` | `FCharacterNetworkMoveDataContainer` |
| 服务端执行 | `ServerCharacterMoveProcessor` | `ServerMove_HandleMoveData/ServerMove_PerformMovement` |
| 误差判断 | 服务端位置/模式比较 | `ServerCheckClientError` |
| Ack/Correction | `CharacterMoveResponse` | `ClientAckGoodMove/ClientAdjustPosition` |
| 重放 | `ClientCharacterPrediction.Replay` | `ClientUpdatePositionAfterServerUpdate` |
| 远端快照 | `CharacterSnapshot` | `FRepMovement` |
| 远端平滑 | `RemoteCharacterInterpolator` | `SmoothCorrection/SmoothClientPosition` |

复刻重点是这条职责链，而不是第一天就把 UE 的所有通用网络类都翻译成 C#。
